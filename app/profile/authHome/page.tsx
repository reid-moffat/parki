import React from 'react';

const AuthHome = () => {
    return (
        <div>
            This page is the top left and bottom left in figma auth (create acc/sign in buttons, and sign in w/ google/email)
        </div>
    );
}

export default AuthHome;
